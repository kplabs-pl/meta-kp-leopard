#@TYPE: Machine
#@DESCRIPTION: Machine configuration for the zynqmp-generic-xczu9eg boards.

#### Preamble
MACHINEOVERRIDES =. "${@['', 'leopard:']['leopard' !='${MACHINE}']}"
#### Regular settings follow

# Yocto device-tree variables
YAML_CONSOLE_DEVICE_CONFIG:pn-device-tree ?= "psu_uart_0"
YAML_MAIN_MEMORY_CONFIG:pn-device-tree ?= "psu_ddr_0"
DT_PADDING_SIZE:pn-device-tree ?= "0x1000"
DTC_FLAGS:pn-device-tree ?= ""
YAML_DT_BOARD_FLAGS ?= "{BOARD template}"

# Yocto u-boot-xlnx variables
UBOOT_MACHINE ?= "xilinx_zynqmp_virt_defconfig"

# Yocto arm-trusted-firmware(TF-A) variables
ATF_CONSOLE_DEFAULT ?= "cadence"
TFA_BL33_LOAD ?= "0x8000000"

# Yocto PMUFW variables
YAML_COMPILER_FLAGS:append:pn-pmu-firmware = " "
YAML_SERIAL_CONSOLE_STDIN:pn-pmu-firmware ?= "psu_uart_0"
YAML_SERIAL_CONSOLE_STDOUT:pn-pmu-firmware ?= "psu_uart_0"

# Yocto FSBL variables
YAML_SERIAL_CONSOLE_STDIN:pn-fsbl-firmware ?= "psu_uart_0"
YAML_SERIAL_CONSOLE_STDOUT:pn-fsbl-firmware ?= "psu_uart_0"

# Yocto KERNEL Variables
UBOOT_ENTRYPOINT  ?= "0x200000"
UBOOT_LOADADDRESS ?= "0x200000"

# zynqmp-generic-xczu9eg Serial Console
SERIAL_CONSOLES ?= "115200;ttyPS0"
YAML_SERIAL_CONSOLE_BAUDRATE ?= "115200"

# Set DDR Base address for u-boot-xlnx-scr variables
DDR_BASEADDR ?= "0x0"
SKIP_APPEND_BASEADDR ?= "0"

# Required generic machine inclusion
require conf/machine/zynqmp-generic.conf
SOC_VARIANT = "eg"

# This is an 'XSCT' based BSP
XILINX_XSCT_VERSION = "2025.1"
XILINX_WITH_ESW = "xsct"

#### No additional settings should be after the Postamble
#### Postamble
PACKAGE_EXTRA_ARCHS:append = "${@['', ' leopard']['leopard' != '${MACHINE}']}"
MACHINE_FEATURES:append = "mali400"
MACHINE_FEATURES:remove = "wifi bluetooth"

# Do not install generated by xilinx boot.bin in /boot/ in rootfs
PREFERRED_PROVIDER_virtual/boot-bin = "bootbin-common"

# Do not install generated by xilinx boot.scr and u-boot binaries in /boot/ in rootfs
MACHINE_ESSENTIAL_EXTRA_RDEPENDS:remove = "u-boot-xlnx-scr u-boot-xlnx-bin bootbin-common"

# Ensure device tree is available in the boot partition (f.e. required by nand scripts)
IMAGE_BOOT_FILES += " \
    ${@bb.utils.contains('PREFERRED_PROVIDER_virtual/dtb', 'device-tree', 'system.dtb', '', d)} \
"